namespace Singularity.Graph
{
    /// <summary>
    /// Provides an enum to differ between whether the edge faces inwards the node or outwards the node.
    /// </summary>
    public enum EEdgeFacing
    {
        Inwards,
        Outwards

    }
}
